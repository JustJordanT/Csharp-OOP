using System;
using static System.DateTime;

namespace StopWatch
{
    public class Start
    {
        public DateTime StartTimer()
        {
            DateTime startTime = DateTime.Now;
            
            return startTime;
        }
    }
}